# 服务器配置
PORT=5200
NODE_ENV=development

# 数据库配置
DB_TYPE=postgres  # postgres 或 mysql
DB_HOST=localhost
DB_PORT=5432
DB_NAME=moetrip
DB_USER=postgres
DB_PASSWORD=postgres
DB_SCHEMA=public

# JWT配置
JWT_SECRET=your-secret-key
JWT_EXPIRES_IN=24h   # 24小时过期

# 文件上传配置
UPLOAD_DIR=uploads
MAX_FILE_SIZE=5242880  # 5MB

# 跨域配置
CORS_ORIGIN=http://localhost:3000
